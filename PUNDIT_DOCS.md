# Pundit Policy Documentation
This document is auto-generated by `rake docs:pundit`.
Generated: 2025-10-26 20:28:52

## AdminDashboard Policy (`AdminDashboardPolicy`)

### `index?`

```ruby
def index? = can?("admin.access")
end
```

## Branch Policy (`BranchPolicy`)

### `create?`

```ruby
def create? = user.can?("branches.write") || user.can?("admin.access")
def edit?   = update?
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `destroy?`

```ruby
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `edit?`

```ruby
def edit?   = update?
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `index?`

```ruby
def index?  = user.can?("branches.read")  || user.can?("admin.access")
def show?   = index?
def new?    = create?
def create? = user.can?("branches.write") || user.can?("admin.access")
def edit?   = update?
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `new?`

```ruby
def new?    = create?
def create? = user.can?("branches.write") || user.can?("admin.access")
def edit?   = update?
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `show?`

```ruby
def show?   = index?
def new?    = create?
def create? = user.can?("branches.write") || user.can?("admin.access")
def edit?   = update?
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

### `update?`

```ruby
def update? = user.can?("branches.write") || user.can?("admin.access")
def destroy? = user.can?("branches.admin") || user.can?("admin.access")

class Scope < Scope
def resolve
user.can?("branches.read") || user.can?("admin.access") ? scope.all : scope.none
end
end
```

## SystemAdmin Policy (`SystemAdminPolicy`)

### `access?`

```ruby
def access?
user&.system_admin?
end
```

## User Policy (`UserPolicy`)

### `create?`

```ruby
def create?; false; end
def update?; record.id == user&.id; end
def destroy?; false; end

def permitted_attributes
[ :email, :name, :time_zone ]
end
```

### `destroy?`

```ruby
def destroy?; false; end

def permitted_attributes
[ :email, :name, :time_zone ]
end
```

### `index?`

```ruby
def index?; false; end
def show?; record.id == user&.id; end
def create?; false; end
def update?; record.id == user&.id; end
def destroy?; false; end

def permitted_attributes
[ :email, :name, :time_zone ]
end
```

### `show?`

```ruby
def show?; record.id == user&.id; end
def create?; false; end
def update?; record.id == user&.id; end
def destroy?; false; end

def permitted_attributes
[ :email, :name, :time_zone ]
end
```

### `update?`

```ruby
def update?; record.id == user&.id; end
def destroy?; false; end

def permitted_attributes
[ :email, :name, :time_zone ]
end
```
